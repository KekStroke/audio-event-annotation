import type { Label } from "@audio/shared";

// Placeholder to verify cross-workspace import
const defaultLabel: Label = { id: "event", name: "Event", color: "#3b82f6" };

console.log("Web placeholder loaded with label:", defaultLabel.name);
