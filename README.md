# Audio Event Annotations

Веб-приложение для ручной разметки аудио-датасетов с визуализацией waveform и спектрограмм.

## Описание

Этот инструмент позволяет:
- 📂 Загружать большие аудиофайлы (несколько гигабайт) из файловой системы
- 📊 Визуализировать аудио в виде waveform и спектрограмм
- 🎧 Воспроизводить аудио и выделенные участки
- 🏷️ Размечать события (events) на временной шкале
- 💾 Сохранять и экспортировать аннотации в JSON/CSV

## Технологии

- **Backend**: Python 3.8+, Flask, SQLAlchemy
- **Audio Processing**: librosa, soundfile, pydub
- **Database**: SQLite
- **Frontend**: HTML5, JavaScript, Wavesurfer.js
- **Testing**: pytest, pytest-bdd

## Установка

### Требования

- Python 3.8 или выше
- pip
- ffmpeg (для обработки MP3 и других форматов)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd audio_event_annotations
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Инициализируйте базу данных:
```bash
python init_db.py
```

## Запуск

Для запуска приложения в режиме разработки:

```bash
python run.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Использование

1. **Загрузка файла**: На главной странице выберите аудиофайл из вашей файловой системы
2. **Просмотр**: Откроется страница с waveform и спектрограммой
3. **Выделение участка**: Зажмите и протяните мышью на waveform для создания региона
4. **Создание аннотации**: 
   - Выделите участок
   - Выберите тип события
   - Добавьте заметки (опционально)
   - Сохраните аннотацию
5. **Экспорт**: Экспортируйте все аннотации в JSON или CSV

## Поддерживаемые форматы

- WAV
- MP3
- FLAC
- OGG

## Структура проекта

```
audio_event_annotations/
├── app/                    # Основное приложение
│   ├── models/            # SQLAlchemy модели
│   ├── routes/            # Flask blueprints (API endpoints)
│   ├── services/          # Бизнес-логика
│   └── utils/             # Вспомогательные функции
├── static/                # Статические файлы (CSS, JS)
├── templates/             # HTML шаблоны
├── tests/                 # Тесты
│   ├── features/         # BDD feature файлы
│   └── step_defs/        # Step definitions
├── docs/                  # Документация
│   ├── PROJECT_CONTEXT.md
│   └── MVP_BACKLOG.md
├── config.py              # Конфигурация приложения
├── requirements.txt       # Python зависимости
└── run.py                # Точка входа
```

## Разработка

Проект следует BDD-методологии. Перед добавлением новой функциональности:

1. Создайте `.feature` файл в `tests/features/`
2. Напишите сценарии (scenarios)
3. Реализуйте step definitions
4. Реализуйте функциональность
5. Убедитесь, что тесты проходят

Запуск тестов:
```bash
pytest tests/
```

Запуск с coverage:
```bash
pytest --cov=app tests/
```

## Ограничения

- Максимальный размер файла: 500 строк кода
- При превышении требуется рефакторинг на модули

## Roadmap

См. [MVP_BACKLOG.md](docs/MVP_BACKLOG.md) для детального плана разработки.

Основные вехи:
1. ✅ Инфраструктура и модели
2. 🚧 Backend API (в процессе)
3. ⏳ Frontend интерфейс
4. ⏳ Тестирование и документация

## Контрибьюция

1. Создайте feature branch
2. Следуйте BDD подходу
3. Убедитесь, что все тесты проходят
4. Создайте Pull Request

## Лицензия

MIT License

## Контакты

Вопросы и предложения: [GitHub Issues](../../issues)

