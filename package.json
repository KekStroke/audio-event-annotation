{
  "name": "audio-event-annotation-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -b tsconfig.build.json --pretty false --verbose",
    "dev": "echo 'Dev placeholder: start server and web once implemented'"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}

